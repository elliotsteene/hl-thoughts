# Create fastapi application

A skeleton fastAPI application running with uvicorn. It should use factory pattern to create the app and two routes for /health and /readiness. The routes should be versioned using /api/v1. async context manager lifespan function should be created